# SPDX-FileCopyrightText: The helm-charts Authors
# SPDX-License-Identifier: 0BSD

mode: split

# The board plus a live IMAP --watch ingester.
ingest:
  enabled: true
  protocol: imap
  signingKeySecret:
    name: tik-signing-key
  passwordSecret:
    name: tik-mailbox-password
