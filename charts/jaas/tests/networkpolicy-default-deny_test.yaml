# SPDX-FileCopyrightText: The helm-charts Authors
# SPDX-License-Identifier: 0BSD

suite: test jaas default-deny NetworkPolicy
templates:
  - networkpolicy-default-deny.yaml
tests:
  - it: is empty by default
    asserts:
      - hasDocuments:
          count: 0

  - it: is empty when networkPolicy is enabled but defaultDeny is off
    set:
      networkPolicy:
        enabled: true
    asserts:
      - hasDocuments:
          count: 0

  - it: is empty when defaultDeny is on but networkPolicy is disabled
    set:
      networkPolicy:
        enabled: false
        defaultDeny:
          enabled: true
    asserts:
      - hasDocuments:
          count: 0

  - it: renders a vanilla deny-all for the kubernetes engine
    set:
      networkPolicy:
        enabled: true
        defaultDeny:
          enabled: true
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: NetworkPolicy
      - equal:
          path: apiVersion
          value: networking.k8s.io/v1
      - equal:
          path: spec.podSelector
          value: {}
      # Egress is NOT denied by default: the allowlist only renders egress rules
      # when networkPolicy.egress.enabled, so denying it here with no re-admit
      # would cut the operator off from DNS + the apiserver.
      - equal:
          path: spec.policyTypes
          value:
            - Ingress
      - notExists:
          path: spec.ingress
      - notExists:
          path: spec.egress

  - it: denies egress too once networkPolicy.egress.enabled (kubernetes)
    set:
      networkPolicy:
        enabled: true
        defaultDeny:
          enabled: true
        egress:
          enabled: true
    asserts:
      - equal:
          path: spec.policyTypes
          value:
            - Ingress
            - Egress

  - it: renders a cilium default-deny for the cilium engine
    set:
      networkPolicy:
        enabled: true
        engine: cilium
        defaultDeny:
          enabled: true
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: CiliumNetworkPolicy
      - equal:
          path: spec.endpointSelector
          value: {}
      # Empty ingress SECTION triggers default deny-mode while admitting nothing.
      # An empty rule ENTRY (- {}) would instead match all peers and allow
      # everything, so the section must be an empty list. Egress is omitted by
      # default (not denied) — see the kubernetes case.
      - equal:
          path: spec.ingress
          value: []
      - notExists:
          path: spec.egress
      - notExists:
          path: spec.enableDefaultDeny

  - it: renders a calico deny-all for the calico engine
    set:
      networkPolicy:
        enabled: true
        engine: calico
        defaultDeny:
          enabled: true
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: NetworkPolicy
      - equal:
          path: apiVersion
          value: projectcalico.org/v3
      - equal:
          path: spec.selector
          value: all()
      - equal:
          path: spec.order
          value: 2000
      - equal:
          path: spec.types
          value:
            - Ingress
      - notExists:
          path: spec.ingress
      - notExists:
          path: spec.egress

  - it: honours a custom order on the calico deny-all
    set:
      networkPolicy:
        enabled: true
        engine: calico
        defaultDeny:
          enabled: true
          order: 5000
    asserts:
      - equal:
          path: spec.order
          value: 5000

  - it: renders a ClusterNetworkPolicy deny-all that loses to the allowlist
    set:
      networkPolicy:
        enabled: true
        engine: clusterNetworkPolicy
        defaultDeny:
          enabled: true
    release:
      namespace: jaas-system
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: ClusterNetworkPolicy
      - equal:
          path: metadata.name
          value: jaas-default-deny-jaas-system
      - equal:
          path: spec.tier
          value: Baseline
      # Lower priority numbers win; the allowlist defaults to
      # clusterNetworkPolicy.priority (1000), so the deny-all's defaultDeny.order
      # (2000) yields.
      - equal:
          path: spec.priority
          value: 2000
      - equal:
          path: spec.subject.namespaces.matchLabels["kubernetes.io/metadata.name"]
          value: jaas-system
      - contains:
          path: spec.ingress
          content:
            name: deny-all
            action: Deny
            from:
              - namespaces: {}
      # Egress omitted by default (not denied) — see the kubernetes case.
      - notExists:
          path: spec.egress

  - it: honours a custom order as the clusterNetworkPolicy priority
    set:
      networkPolicy:
        enabled: true
        engine: clusterNetworkPolicy
        defaultDeny:
          enabled: true
          order: 9000
    asserts:
      - equal:
          path: spec.priority
          value: 9000
